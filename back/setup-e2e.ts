// setup-e2e.ts
// Place ici tes hooks globaux Jest pour les tests e2e si besoin.
// Exemple : beforeAll, afterAll, setup de base de données, etc.

// Exemple minimal :
beforeAll(async () => {
  // Setup global e2e (optionnel)
});

afterAll(async () => {
  // Cleanup global e2e (optionnel)
});
