

/// This Integration Test starts the backend server
/// and runs end-to-end tests against the remote data.
/// The app connects to its endpoints to perform registration and login
/// and then performs a series of actions (creating a plan, going to the detail of a plan)
void main() {
