import 'package:front/data/services/api/model/user/user_api_model.dart';
import 'package:front/domain/models/user/user.dart';

const kUser = User(username: 'USERNAME', email: 'EMAIL');

const userApiModel = UserApiModel(
    id: 'USER_ID', username: 'USERNAME', email: 'EMAIL', isPremium: false);
